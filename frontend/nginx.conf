server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # For single-page app: all routes serve index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
